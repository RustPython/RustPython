[package]
name = "example-barebone"
version = "0.1.0"
edition = "2021"

[dependencies]
rustpython-vm = { path = "../../crates/vm", default-features = false, features = ["stdio"] }

[workspace]

[patch.crates-io]
